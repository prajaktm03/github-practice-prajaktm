name: 04-using-actions

on: push

jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout the code 
              uses: actions/checkout@v4 
            - name: prinitng folders
              run: ls
               

        # 1. install reacts dependencies
        # 2. run automated test

        